{
  "name": "lms-plugin-toolguard-current-time",
  "version": "1.0.1",
  "description": "Inject the host's current date, time, and timezone into every LM Studio user turn and expose it as a tool.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "lms dev",
    "install-plugin": "lms dev --install"
  },
  "license": "MIT",
  "dependencies": {
    "@lmstudio/sdk": "1.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.8"
  }
}
